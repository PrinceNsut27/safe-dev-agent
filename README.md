# SafeDev Agent

An intent-aware autonomous file system agent.

## Features
- Structured intent model
- Policy-based runtime enforcement
- Separation between reasoning and execution
- Demonstrates allowed and blocked actions

## Run

pip install -r requirements.txt
python main.py